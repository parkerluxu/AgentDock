# AgentDock DSH integration

`@agentdock/dsh` adds AgentDock as a DeepSeek Harness provider and Settings extension. It depends on the separately published `agentdock` core package.

Build a tarball with `npm pack --workspace @agentdock/dsh`, then install it into a DSH Web profile with the DSH plugin command.
