# AgentDock core

`agentdock` provides the AgentDock CLI, local API, runtime, configuration model, storage, and SDK. It intentionally has no DSH, Cordis, or React dependency.

Install `@agentdock/dsh` separately when DeepSeek Harness integration is needed.
