{
  "name": "agentdock-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace agentdock && npm run build --workspace @agentdock/dsh",
    "typecheck": "npm run typecheck --workspace agentdock && npm run typecheck --workspace @agentdock/dsh",
    "test": "npm run test --workspace agentdock && npm run test --workspace @agentdock/dsh",
    "test:watch": "npm run test:watch --workspace agentdock",
    "config:validate": "npm run config:validate --workspace agentdock --",
    "wiki:dev": "vitepress dev docs/wiki",
    "wiki:build": "vitepress build docs/wiki",
    "wiki:preview": "vitepress preview docs/wiki"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
